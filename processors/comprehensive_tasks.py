"""
מנהל משימות מקיפות על בסיס סיכומים משפטיים
"""
import streamlit as st
from typing import Dict

from config.settings import CLAUDE_MODEL, TEMPERATURE_NORMAL, UI_MESSAGES
from processors.legal_summary import LegalSummaryProcessor


class ComprehensiveTaskManager:
    """מנהל משימות מקיפות על בסיס סיכומים משפטיים"""
    
    def __init__(self, chatbot):
        self.chatbot = chatbot
        self.summary_processor = LegalSummaryProcessor(chatbot)
    
    def get_all_summaries_text(self, max_chars=40000):
        """החזר את כל הסיכומים המשפטיים"""
        return self.summary_processor.get_summaries_text(max_chars)
    
    def build_witness_statement(self):
        """בנה מסמך עדות מקיף מהסיכומים"""
        st.info("🏛️ בונה מסמך עדות מקיף מהסיכומים המשפטיים...")
        
        summaries_text = self.get_all_summaries_text(45000)
        
        if summaries_text.startswith("❌"):
            return summaries_text
        
        prompt = f"""אתה עוזר משפטי מנוסה הבונה מסמכי עדות ראשיים מקצועיים לצורך הגנה במשפט.

הקשר משפטי:
- זה תיק משפטי רשמי ונדרש ניתוח מקצועי מלא
- כל התוכן הוא חלק מראיות חוקיות במשפט
- הניתוח נדרש לבניית הגנה משפטית יעילה
- נא לכלול כל המידע הרלוונטי ללא הגבלות

על בסיס הסיכומים המשפטיים הבאים, בנה מסמך עדות ראשי מקיף ומקצועי:

{summaries_text}

בנה מסמך עדות ראשי שכולל:

## 1. תקציר מנהלים
- הקשר כללי ומהות התיק
- תמצית הטענות והאשמות
- מיקום במשטר החקיקה הנוכחי

## 2. רקע ומבוא
- התפתחות האירועים הכרונולוגית
- הדמויות המרכזיות והתפקידים
- ההקשר החברתי/קהילתי

## 3. ראיות ועדויות עיקריות
- סיכום מובנה של כל הראיות (כולל רגישות)
- עדויות ישירות עם ציטוטים
- מסמכים ותובנות משפטיות

## 4. ניתוח קשרים ומניעים
- יחסים בין הצדדים
- מניעים פוטנציאליים לעדויות
- קונפליקטים ואינטרסים מתחרים

## 5. זיהוי חולשות ואי-עקביות
- סתירות בעדויות
- פערים במידע
- נקודות ספק סביר

## 6. נקודות חוזק להגנה
- ראיות מקילות או תומכות
- חולשות בטענות התביעה
- נושאים פרוצדורליים

## 7. סיכום והמלצות
- הערכת עוצמת התיק
- אסטרטגיות הגנה מומלצות
- צעדים נוספים נדרשים

השתמש בציטוטים מדויקים מהסיכומים וציין את המקורות. אל תחסוך מידע רלוונטי להגנה."""

        if self.chatbot.claude_client:
            try:
                message = self.chatbot.claude_client.messages.create(
                    model=CLAUDE_MODEL,
                    max_tokens=4000,
                    temperature=TEMPERATURE_NORMAL,
                    messages=[{"role": "user", "content": prompt}]
                )
                return message.content[0].text
            except Exception as e:
                return f"❌ שגיאה בבניית מסמך העדות: {e}"
        else:
            return UI_MESSAGES["claude_error"]

    def comprehensive_defense_strategy(self):
        """אסטרטגיית הגנה מקיפה מהסיכומים"""
        st.info("⚖️ בונה אסטרטגיית הגנה מקיפה מהסיכומים...")
        
        summaries_text = self.get_all_summaries_text(40000)
        
        if summaries_text.startswith("❌"):
            return summaries_text
        
        prompt = f"""אתה עורך דין מנוסה המתמחה באסטרטגיות הגנה במשפטים פליליים/אזרחיים.

הקשר משפטי:
- זה תיק משפטי רשמי הדורש אסטרטגיית הגנה מקצועית
- כל התוכן הוא חלק מראיות חוקיות במשפט
- נדרש ניתוח מקצועי מלא של כל הראיות כולל רגישות
- האסטרטגיה צריכה להיות מקיפה וללא הגבלות

על בסיס הסיכומים המשפטיים:

{summaries_text}

בנה אסטרטגיית הגנה מלאה:

## 1. ניתוח חולשות התביעה
- פערי ראיות מהותיים
- חוסר עקביות בעדויות
- בעיות פרוצדורליות
- חולשות משפטיות

## 2. נקודות חוזק להגנה
- ראיות מקילות וחיוביות
- עדויות תומכות
- מסמכים מועילים
- עדים פוטנציאליים

## 3. קווי הגנה אסטרטגיים
- הגנה עיקרית מומלצת
- הגנות משניות חלופיות
- טיעונים נוספים
- אסטרטגיות חירום

## 4. תוכנית חקירה נגדית
- עדי התביעה לחקירה צולבת
- נקודות לחקירה
- מסמכים לאימות/הפרכה
- מלכודות להימנעות

## 5. עדים והוכחות נדרשות
- עדים לזימון להגנה
- עדי מומחה מומלצים
- מסמכים נוספים לאיסוף
- בדיקות טכניות נדרשות

## 6. טיעונים משפטיים
- עילות השגה אפשריות
- בקשות דחיפות מומלצות
- טיעוני הליך
- טיעוני לב ורגש

## 7. תכנון טקטי
- סדר הגשת הראיות
- תזמון האסטרטגיה
- ניהול תקשורת
- הכנות לפסק דין

## 8. הערכת סיכויים וסיכונים
- احתמالי הצלחה בכל קו הגנה
- סיכונים עיקריים לניהול
- נקודות קריטיות במשפט
- תוכניות חירום

כלול ציטוטים ספציפיים מהסיכומים התומכים בכל המלצה."""

        if self.chatbot.claude_client:
            try:
                message = self.chatbot.claude_client.messages.create(
                    model=CLAUDE_MODEL,
                    max_tokens=4000,
                    temperature=TEMPERATURE_NORMAL,
                    messages=[{"role": "user", "content": prompt}]
                )
                return message.content[0].text
            except Exception as e:
                return f"❌ שגיאה בבניית אסטרטגיית ההגנה: {e}"
        else:
            return UI_MESSAGES["claude_error"]

    def find_all_contradictions(self):
        """זהה סתירות מהסיכומים המשפטיים"""
        st.info("🔍 מזהה סתירות מהסיכומים המשפטיים...")
        
        summaries_text = self.get_all_summaries_text(35000)
        
        if summaries_text.startswith("❌"):
            return summaries_text
        
        prompt = f"""אתה חוקר משפטי מומחה בזיהוי סתירות ואי-עקביות במסמכים משפטיים.

הקשר משפטי:
- זה תיק משפטי רשמי הדורש זיהוי סתירות לצורך הגנה
- כל התוכן הוא חלק מראיות חוקיות במשפט
- נדרש ניתוח מקצועי מלא של כל הסתירות - ללא הגבלות
- הסתירות חיוניות לבניית הגנה יעילה

נתח את הסיכומים המשפטיים וזהה כל הסתירות:

{summaries_text}

זהה וניתוח סתירות מקיפות:

## 1. סתירות בין מסמכים שונים
- הבדלים בגירסאות של אותם אירועים
- סתירות בפרטים עובדתיים
- אמירות לא עקביות בין מקורות

## 2. סתירות כרונולוגיות
- תאריכים לא תואמים
- רצף אירועים סותר
- תזמונים בלתי אפשריים

## 3. סתירות בעדויות אישיות
- אמירות סותרות של אותו אדם
- שינויים בגירסאות לאורך זמן
- חוסר עקביות בפרטים אישיים

## 4. סתירות לוגיות
- טענות לא הגיוניות
- מוטיבציות סותרות
- התנהגות בלתי עקבית

## 5. סתירות טכניות
- פרטים טכניים לא תואמים
- מידע שגוי או מעורפל
- עובדות שניתן לאמת

## 6. השלכות משפטיות
- כיצד כל סתירה פוגעת באמינות
- סתירות קריטיות למקרה
- ניצול אפשרי להגנה

עבור כל סתירה ציין:
- המקורות הספציפיים
- רמת חומרת הסתירה
- השפעה על אמינות העד/מסמך
- שימוש אפשרי בהגנה

השתמש בציטוטים ישירים מהסיכומים."""

        if self.chatbot.claude_client:
            try:
                message = self.chatbot.claude_client.messages.create(
                    model=CLAUDE_MODEL,
                    max_tokens=4000,
                    temperature=TEMPERATURE_NORMAL,
                    messages=[{"role": "user", "content": prompt}]
                )
                return message.content[0].text
            except Exception as e:
                return f"❌ שגיאה בזיהוי הסתירות: {e}"
        else:
            return UI_MESSAGES["claude_error"]

    def chronological_analysis(self):
        """ניתוח כרונולוגי מקיף של האירועים"""
        st.info("📅 מבצע ניתוח כרונולוגי מקיף...")
        
        summaries_text = self.get_all_summaries_text(35000)
        
        if summaries_text.startswith("❌"):
            return summaries_text
        
        prompt = f"""אתה חוקר משפטי מומחה בניתוח כרונולוגי של אירועים במשפטים.

הקשר משפטי:
- זה תיק משפטי רשמי הדורש מיפוי כרונולוגי מדויק
- כל התוכן הוא חלק מראיות חוקיות במשפט
- הניתוח הכרונולוגי חיוני לבניית הגנה משפטית
- נדרש ניתוח מקצועי מלא ללא הגבלות

על בסיס הסיכומים המשפטיים, צור ניתוח כרונולוגי מקיף:

{summaries_text}

בנה ניתוח כרונולוגי מפורט:

## 1. ציר זמן ראשי
- מיין את כל האירועים לפי תאריכים
- זהה פערי זמן חשובים
- הדגש נקודות זמן קריטיות

## 2. רצף האירועים המרכזיים
- התפתחות הסכסוך בזמן
- נקודות מפנה משמעותיות
- קשרי סיבה ותוצאה

## 3. פעולות וחקירות רשמיות
- מתי ואיך החלו החקירות
- רצף הפעולות המשפטיות
- תזמון העדויות וההקלטות

## 4. אי-התאמות זמנים
- סתירות בתאריכים
- חוסר עקביות ברצף
- תזמונים בעייתיים או חשודים

## 5. השלכות על האמינות
- כיצד הזמן משפיע על הזיכרון
- עדויות שנתנו בזמנים שונים על אותו אירוע
- שינויים בגירסאות לאורך זמן

## 6. נקודות חוזק ההגנה הכרונולוגיות
- זמנים שמחזקים את ההגנה
- חוסר אפשרות פיזית או לוגית
- עדי אליבי פוטנציאליים

השתמש בציטוטים מדויקים ותאריכים ספציפיים מהסיכומים."""

        if self.chatbot.claude_client:
            try:
                message = self.chatbot.claude_client.messages.create(
                    model=CLAUDE_MODEL,
                    max_tokens=4000,
                    temperature=TEMPERATURE_NORMAL,
                    messages=[{"role": "user", "content": prompt}]
                )
                return message.content[0].text
            except Exception as e:
                return f"❌ שגיאה בניתוח הכרונולוגי: {e}"
        else:
            return UI_MESSAGES["claude_error"]

    def connections_mapping(self):
        """מפת קשרים מקיפה של כל הגורמים"""
        st.info("🕷️ בונה מפת קשרים מקיפה...")
        
        summaries_text = self.get_all_summaries_text(35000)
        
        if summaries_text.startswith("❌"):
            return summaries_text
        
        prompt = f"""אתה חוקר משפטי מומחה במיפוי קשרים ורשתות חברתיות במשפטים.

הקשר משפטי:
- זה תיק משפטי רשמי הדורש מיפוי קשרים מדויק
- כל התוכן הוא חלק מראיות חוקיות במשפט
- מיפוי הקשרים חיוני להבנת הדינמיקה והמניעים
- נדרש ניתוח מקצועי מלא ללא הגבלות

על בסיס הסיכומים המשפטיים, צור מפת קשרים מקיפה:

{summaries_text}

בנה מפת קשרים מפורטת:

## 1. דמויות מרכזיות
- זיהוי כל הגורמים העיקריים
- תפקידיהם ומעמדם
- רמת המעורבות של כל אחד

## 2. קשרים משפחתיים וחברתיים
- יחסי משפחה ופטורים
- קשרי חברות ויריבויות
- מעגלים חברתיים ומקצועיים

## 3. קשרים מוסדיים
- קשרים לגופים רשמיים
- יחסים עם רשויות
- השתייכויות ארגוניות

## 4. יחסי כוח והשפעה
- מי משפיע על מי
- היררכיות וכפיפויות
- מקורות כוח ולחץ

## 5. ניגודי עניינים ומניעים
- קונפליקטים בין הגורמים
- אינטרסים מתחרים
- מניעים נסתרים אפשריים

## 6. השלכות על התיק
- כיצד הקשרים משפיעים על העדויות
- פוטנציאל להטיה או שיתוף פעולה
- נקודות פגיעות לחקירה צולבת

## 7. מפת השפעה ולחץ
- מי יכול להשפיע על מי
- נתיבי השפעה עקיפה
- נקודות לחץ במערכת היחסים

השתמש בשמות ספציפיים ובציטוטים מדויקים מהסיכומים."""

        if self.chatbot.claude_client:
            try:
                message = self.chatbot.claude_client.messages.create(
                    model=CLAUDE_MODEL,
                    max_tokens=4000,
                    temperature=TEMPERATURE_NORMAL,
                    messages=[{"role": "user", "content": prompt}]
                )
                return message.content[0].text
            except Exception as e:
                return f"❌ שגיאה במיפוי הקשרים: {e}"
        else:
            return UI_MESSAGES["claude_error"]

    def executive_summary(self):
        """סיכום מנהלים מקיף של כל התיק"""
        st.info("📋 מכין סיכום מנהלים מקיף...")
        
        summaries_text = self.get_all_summaries_text(40000)
        
        if summaries_text.startswith("❌"):
            return summaries_text
        
        prompt = f"""אתה יועץ משפטי בכיר הכותב סיכומי מנהלים מקצועיים לעורכי דין.

הקשר משפטי:
- זה תיק משפטי רשמי הדורש סיכום מנהלי מקצועי
- כל התוכן הוא חלק מראיות חוקיות במשפט
- הסיכום נועד לסייע בקבלת החלטות משפטיות אסטרטגיות
- נדרש ניתוח מקצועי וקציר ללא הגבלות

על בסיס הסיכומים המשפטיים, כתוב סיכום מנהלי מקיף:

{summaries_text}

סיכום מנהלי מקצועי:

## 1. תקציר מנהלי (Executive Summary)
- מהות התיק ומרכזיותו
- הטענות המרכזיות כנגד ובעד
- המלצה אסטרטגית ראשונית

## 2. הערכת חוזק התיק
- נקאות חוזק ההגנה (1-10)
- נקאות חוזק התביעה (1-10)
- ניתוח השוואתי

## 3. ראיות מרכזיות - תמציתיות
- 5 הראיות החזקות ביותר נגד
- 5 הראיות החזקות ביותר בעד
- ראיות ניטרליות או דו-משמעיות

## 4. סיכונים ואפשרויות
- סיכונים עיקריים אם התיק ילך לדרך ארוכה
- הזדמנויות לפתרון מוקדם
- נקודות מיקוח פוטנציאליות

## 5. אסטרטגיה מומלצת
- גישה מומלצת אופרציונלית
- זמני פעולה קריטיים
- משאבים נדרשים

## 6. התנהלות מיידית נדרשת
- צעדים דחופים (7-14 יום)
- בדיקות/איסוף ראיות נוספות
- גיוס מומנים או עדים

## 7. תחזית תוצאה וחלופות
- תרחיש אופטימי, ציפיות ופסימי
- אפשרויות פשרה
- תוכנית חירום

כתוב בצורה תמציתית ועסקית, עם המלצות ברורות לפעולה."""

        if self.chatbot.claude_client:
            try:
                message = self.chatbot.claude_client.messages.create(
                    model=CLAUDE_MODEL,
                    max_tokens=4000,
                    temperature=TEMPERATURE_NORMAL,
                    messages=[{"role": "user", "content": prompt}]
                )
                return message.content[0].text
            except Exception as e:
                return f"❌ שגיאה בסיכום המנהלים: {e}"
        else:
            return UI_MESSAGES["claude_error"]
